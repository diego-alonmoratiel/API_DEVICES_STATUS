apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "device-management-api.fullname" . }}-config
  labels:
    {{- include "device-management-api.labels" . | nindent 4 }}
data:
  LOG_LEVEL: {{ .Values.config.logLevel | quote }}
  DEBUG: {{ .Values.config.debug | quote }}